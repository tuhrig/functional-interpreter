C- und Smalltalk-Style Parser für Interpreter implementieren und als Strategy 
wählen (lassen). Alles als Library kapseln und von GUI trennen. Der Parser 
soll ein neues Interface bekommen. Die File Utilities müssen umziehen und 
eine Syntax-Check method soll kommen die die Line Number zurück gibt wenn 
ein Fehler ge

Punkt vor strich...

if-else und if-ifelse-else

print sucket --> out stream registrieren an den print übergiebt, default ist return string
tests könne zusätzlich den aktuellen print buffer lesen, gui fragt ihn einfach immer ab und printet in rot

begin ist nicht nötig bei lambda!!!!

loops (while, for, do) als neue build-in operationen bauen... ist wohl am einfachsten